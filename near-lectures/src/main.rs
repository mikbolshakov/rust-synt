fn main() {
    // arrays
    let some_array: [u8; 4] = [1, 2, 3, 4];
    // tuples
    // как массив, только могут быть разные элементы в кортеже
    let some_data = (3.14, "Mike", true);
    let pi = some_data.0;
}